syntax = "proto3";

package pb;

// game definition
// Service Service
service Service {
	rpc Stream(stream Frame) returns (stream Frame);  // 透传消息, 双向流
}


// Frame Frame
message Frame {
	bytes Message=1;
}

// Login 登陆
message Login {
	string Cuid = 1;
	string Channel = 2;
	string CName = 3;
}
